name: Clínicas Brasília TestFlight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    uses: socialbot114-cell/jerv/.github/workflows/ios-testflight.yml@main
    with:
      project_dir: .
      scheme: ClinicasBrasilia
      xcode_project: ClinicasBrasilia.xcodeproj
      bundle_id: br.com.clinicas.brasilia
      artifact_name: clinicas-brasilia-ios-ipa
    secrets: inherit
